/**
 * Chat Default Fallback
 * 
 * This component is rendered when the chat parallel route
 * cannot match a more specific page.tsx file.
 * 
 * For the chat area, we want to show the empty state
 * when no specific thread is selected.
 */
export default function ChatDefault() {
  return null;
}
